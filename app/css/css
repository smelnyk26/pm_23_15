.sidebar {
    width: 250px; /* Ширина бокової панелі */
    height: 100%; /* Займати весь висоту вікна */
    background-color: #34495e; /* Колір фону бокової панелі */
    position: fixed; /* Зафіксувати панель при прокручуванні */
    left: 0; /* Розміщення зліва */
    top: 0; /* Розміщення зверху */
}

.sidebar ul {
    padding: 0;
    list-style: none;
}

.sidebar ul li {
    padding: 8px;
}

.sidebar ul li a {
    text-decoration: none;
    color: #88999c;
}

/* Зміщення основного вмісту вправо, щоб не перекривати бокову панель */
.content {
    margin-left: 250px; /* Ширина бокової панелі */
    padding: 20px;
}